# NexusForge API Client

A Python script to interact with the NexusFest Forge backend API.

## Requirements
- `requests` (pip install requests)

## Usage
Set up the `API_URL` and authenticate using your Bearer token, or Worker API key.

```bash
python nexus-forge.py
```
